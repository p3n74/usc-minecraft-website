# DEPRECATED — do not use for Coolify or production.
# Early deploy used an embedded static snapshot. Live builds must use the root
# Dockerfile from a real git repository (see README.md).
# This path remains gitignored if a local copy exists.
